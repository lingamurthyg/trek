# DB credentials for the legacy CLI tooling (etl-runner.jar, schema-fix.sh).
# NOTE: these are duplicated in applicationContext.xml and hibernate.cfg.xml.
# The web app does NOT read this file at runtime. ops scripts do.
# If you change the password, change all three.  miyer 2011

db.driver=com.mysql.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/cargotrak
db.user=cargotrak
db.password=cargotrak123
db.maxConnections=20
